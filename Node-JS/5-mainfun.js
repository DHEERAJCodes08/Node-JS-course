const sayHI = (name) => {
    console.log(`Hello Iam ${name}`);          //its an Normal program  
 }


 module.exports = sayHI;   //since here we only have one property to share we can share it directly !